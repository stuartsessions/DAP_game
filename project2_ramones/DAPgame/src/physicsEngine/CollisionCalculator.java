package physicsEngine;
/*
 * package PhysicsEngine;
 

import gameObjects.Velocity;
import java.lang.Math;

public class CollisionCalculator {
	int speedV;
	int speedH;
	int weight;
	double totalSpeed;
	int newSpeed;
	Velocity velocity;
	double deg;
	CollisionCalculator calculation;
	
	public void getspeedV() {
		speedV = (int) (velocity.getYcomp());
	}
	public void getspeedH() {
		speedH = (int) (velocity.getYcomp());
	}
	public void getweight() {
		weight = (gameObject.getWeight);
			
	}
	public void getDeg() {
		deg = gameObject.getdeg;
	}
	
	public CollisionCalculator() {
		totalSpeed = (Math.pow(speedV, 2) +Math.pow(speedH, 2));
		totalSpeed = Math.sqrt(totalSpeed);
		if (totalSpeed > weight) {
			newSpeed = (int) (totalSpeed - weight);
		}
		else {
			newSpeed = 0;
		}
	}
	public double getnewdeg() {
		return deg;
		
	}
	public double getnewspeedV() {
		return speedV;
		
	}
	public double getnewspeedh() {
		return speedH;
	}
}
*/
